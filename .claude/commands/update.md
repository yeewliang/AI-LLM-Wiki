Read CLAUDE.md first.

Refresh this page with any new information: $ARGUMENTS

Rules:
- Preserve existing structure and content
- Only add or correct — do not remove existing knowledge
- Update the updated: frontmatter field to today's date
- Log what changed and why in wiki/log.md

Then run:
git add .
git commit -m "update: $ARGUMENTS"
git push